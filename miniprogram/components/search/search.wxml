<view class="container">
<input type="input" placeholder="{{placeholder}}" 
   value='{{inputValue}}' bindinput="onInput" bindfocus="onFocus"></input>
   <button class="search-btn" bind:tap="onSearch">搜索</button>
</view>
